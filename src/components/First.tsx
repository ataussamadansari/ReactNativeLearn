import React from "react";
import { View, Text } from "react-native";

const First = () => {
    return(
        <View>
            <Text style={{color:'#fff', fontSize: 16}}>Hello First</Text>
        </View>
    );
};

export default First;
